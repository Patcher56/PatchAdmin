Generelle Regeln:
Runde Klammern = MUSS
Eckige Klammern = OPTIONAL

Alle Befehle beginnen mit einem Rufezeichen!


chat command style:
!(COMMAND) [PLAYER/ARGS] [ARGS]

PLUGIN CREATION:

local Plugin = {
	
	name = "PLUGINNAME",
	command = "PLUGINCOMMAND",
	alias = { "ALIAS1", "ALIAS2" }, --you can add as many as you want
	args_required = { "ARG1", "ARG2" }, --you can add as many as you want
	args_optional = { "OPTIONAL1", "OPTIONAL2" } --you can add as many as you want

}

function Plugin:Call( ply, args )

	--THERE GOES YOUR STUFF

end

sv_PAdmin.AddPlugin( Plugin )

ARGUMENT KEYWORDS:

PLAYER_SetAUniqueIDHere - PAdmin searches for the players and gives you the player-entity